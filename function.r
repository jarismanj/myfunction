add2 <- function(x,y) {
  x+y
}

above10 <- function(x) {
  use <- x > 10
  x[use]
}

x <- 1:20
above(x)

columnmean <- function(y {
  mc <- ncol(y)
  means <- numeric(nc)
  for(i in 1:nc) {
    means[i]<means(y,[l]<mean(y, [, i]))
  }
  means }
}
